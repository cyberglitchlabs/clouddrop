---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: soulsync
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: bjw-s-app-template
    namespace: flux-system
  dependsOn:
    - name: slskd
  values:
    controllers:
      soulsync:
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: boulderbadgedad/soulsync
              tag: "2.6.4"
            env:
              TZ: America/Chicago
              PUID: 568
              PGID: 568
              UMASK: "022"
              FLASK_ENV: production
              PYTHONPATH: /app
              SOULSYNC_CONFIG_PATH: /config/config.json
              GUNICORN_CMD_ARGS: "--workers=2 --threads=24"
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 8008
                  initialDelaySeconds: 180
                  periodSeconds: 30
                  timeoutSeconds: 10
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 8008
                  initialDelaySeconds: 120
                  periodSeconds: 30
                  timeoutSeconds: 10
                  failureThreshold: 3
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                memory: 1Gi
    defaultPodOptions:
      securityContext:
        fsGroup: 568
        fsGroupChangePolicy: "OnRootMismatch"
        supplementalGroups: [65539]
    service:
      app:
        controller: soulsync
        ports:
          http:
            port: 8008
          spotify:
            port: 8888
          tidal:
            port: 8889
    persistence:
      config:
        enabled: true
        type: persistentVolumeClaim
        existingClaim: soulsync-config-nfs-v2
        globalMounts:
          - path: /config
          - path: /app/data
      media:
        enabled: true
        type: persistentVolumeClaim
        existingClaim: soulsync-media-smb
        globalMounts:
          - path: /media
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
