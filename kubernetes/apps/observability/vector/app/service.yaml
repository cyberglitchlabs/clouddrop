---
apiVersion: v1
kind: Service
metadata:
  name: vector-tcp
  namespace: observability
  annotations:
    lbipam.cilium.io/ips: "192.168.42.12"
    lbipam.cilium.io/sharing-key: "vector-syslog"
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
  selector:
    app.kubernetes.io/name: vector
  ports:
    - name: syslog-tcp
      port: 514
      targetPort: 6514
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: vector-udp
  namespace: observability
  annotations:
    lbipam.cilium.io/ips: "192.168.42.12"
    lbipam.cilium.io/sharing-key: "vector-syslog"
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
  selector:
    app.kubernetes.io/name: vector
  ports:
    - name: syslog-udp
      port: 514
      targetPort: 6514
      protocol: UDP
